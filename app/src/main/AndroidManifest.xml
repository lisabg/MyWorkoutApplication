<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.example.myapplication">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher_workout"
            android:label="Workout"
            android:roundIcon="@mipmap/ic_launcher_workout_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".exercise.ExerciseDetailsActivity"></activity>
        <activity android:name=".stretch.StretchActivity"></activity>
        <activity android:name=".exercise.ExerciseActivity"></activity>
        <activity android:name=".converter.ConverterActivity"></activity>
        <activity android:name=".DashboardActivity"></activity>
        <activity
                android:name=".MainActivity"
                android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>